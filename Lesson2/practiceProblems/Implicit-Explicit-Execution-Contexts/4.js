//What build in methods have we learned about that we can use to
//specify a functions execution context explicitly?

/*
We have learned about `call` and `apply`, `call` being the primary one,
since you can use call and spread syntax to replicate `apply`.

Call and apply let us explicitly set the function execution context.
*/